(function(){"use strict";
function current(){try{return JSON.parse(sessionStorage.getItem("santivanez_private_session_v1"))||{usuario:"sistema",rol:"Sistema"};}catch(_){return{usuario:"sistema",rol:"Sistema"};}}
function log(accion,resultado){const session=current(),items=window.PrivateStorage.list("audit");items.unshift({id:"AUD-"+String(Date.now()),fecha:new Date().toISOString(),usuario:session.usuario,rol:session.rol,accion:String(accion).slice(0,180),resultado:resultado||"Correcto"});window.PrivateStorage.save("audit",items.slice(0,500));}
function render(){const body=document.querySelector("#audit-body");if(!body)return;const query=(document.querySelector("#audit-search")||{}).value||"";const value=query.toLowerCase();const items=window.PrivateStorage.list("audit").filter((row)=>Object.values(row).some((v)=>String(v).toLowerCase().includes(value)));body.replaceChildren();if(!items.length){const tr=document.createElement("tr"),td=document.createElement("td");td.colSpan=5;td.className="empty";td.textContent="No hay registros para el filtro indicado.";tr.appendChild(td);body.appendChild(tr);return;}items.forEach((row)=>{const tr=document.createElement("tr");[new Date(row.fecha).toLocaleString("es-PE"),row.usuario+" · "+row.rol,row.accion,row.resultado,row.id].forEach((value,index)=>{const td=document.createElement("td");td.textContent=value;if(index===4)td.className="code";tr.appendChild(td);});body.appendChild(tr);});}
document.addEventListener("DOMContentLoaded",()=>{const search=document.querySelector("#audit-search");if(search)search.addEventListener("input",render);if(document.querySelector("#audit-body")){log("Consultó la bitácora de auditoría","Correcto");render();}});
window.PrivateAudit={log,render};
})();
